import React from 'react';
import ReactDOM from 'react-dom/client';
import ChecklistJournal from './checklist_simple';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ChecklistJournal />
  </React.StrictMode>
);
